route {
    host = "example.com"
    path = "/simple"
    type = "http"
}

route {
    host = "example.com"
    path = "/substitutions"
    type = "http"
}

route {
    host = "example.com"
    path = "/users/:name"
    type = "http"
}
